{
  "name": "@ipjohnson-org/canaryspec-framework",
  "version": "0.1.0",
  "description": "CanarySpec synthetic test framework — assertions, retry, timing, fixtures",
  "type": "module",
  "main": "dist/framework.esm.js",
  "types": "dist/index.d.ts",
  "files": ["dist/", "libs/"],
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
